package day06;

public class Method01 {

	// 메서드 생성 가능 위치
	
	// 두 정수를 주면 (매개변수) 합을 리턴하는 메서드
	/* 매개변수 : 두 정수 (int num1, int num2)
	   리턴타입 : 합 => int
	   메서드명 : sum
	   메서드에서 리턴값을 리턴할 때 쓰는 키워드 : return 값;
	 */
	public static int sum(int num1, int num2) {
		int hap = num1 + num2;
		return hap;
	}
	
	public static void main(String[] args) {
		
		// 메서드 호출
		int hap = sum(15,74);
		System.out.println(hap);
		
		
		
		/* method = 기능 = 함수 = function
		 * class : 변수(가지는 요소), 메서드(해야하는 기능)
		 * main : 컴파일러가 실행을 위해 가장 먼저 찾는 메서드
		 
		 * method 구조
		 	: 접근제어자 리턴타입 메서드명(매개변수){
		 		구현;
		 	  }
		   - 접근제어자 : 접근할 수 있는 주체의 제한범위
		   - 리턴타입 : 기능이 실행 된 후 반환 값 (반환할 값의 자료형) => 반드시 1개
		   - void : 리턴할 값이 없음 -> 리턴타입이 void로 표시
		   - 메서드명 : 소문자로만 시작
		   - 매개변수(= 파라미터) : 해당 기능을 하기 위해 외부에서 반드시 들어와야하는 값 -> 여러개 가능
		   
		 * 메서드는 중복이 안된다 (메서드 안에 메서드가 있을 수 없음)
		 * 메서드 선언 위치
		   - 클래스 안, 다른 메서드 밖
		   - 메서드는 독립적 기능을 함.
		   - 메서드의 순서는 아무 상관이 없음.
		   - 메서드는 호출되어야만 실행됨.
		 */
	}
	
}
